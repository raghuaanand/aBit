import React from 'react'

const Stats = (props) => {
  return (
    <div>
      <p>{props.number}</p>
      <p>{props.paraOne}</p>
      <p>{props.paraTwo} </p>
    </div>
  )
}

export default Stats
